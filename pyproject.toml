[project]
name = "knowledge-base"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "PyYAML>=6.0,<7",
]

[dependency-groups]
dev = [
  "pre-commit>=4.3,<5",
]

[tool.uv]
package = false
